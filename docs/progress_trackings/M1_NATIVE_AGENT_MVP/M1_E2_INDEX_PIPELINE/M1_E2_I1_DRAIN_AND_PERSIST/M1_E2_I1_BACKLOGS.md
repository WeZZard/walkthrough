# Backlogs — M1 E2 I1 Drain and Persist

- Open output file and write header on controller startup
- Batch drain index ring and write fixed records
- Periodic flush or flush on shutdown
- Update stats counters: events, bytes, cycles, drops
