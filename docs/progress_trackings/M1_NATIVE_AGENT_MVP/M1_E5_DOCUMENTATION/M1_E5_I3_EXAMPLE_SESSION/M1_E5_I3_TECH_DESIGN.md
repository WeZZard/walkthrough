# Tech Design — M1 E5 I3 Example Session

## Objective
Provide a deterministic example tracing session with golden outputs for fast validation.

## Contents
- Scripted run against test_cli with fixed duration and output dir
- Captured outputs: summarize text, header bytes (small), counts
- Placement: tracer_backend/tests/fixtures/golden/* (small text/binary only)
